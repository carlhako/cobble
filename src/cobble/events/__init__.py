"""Interpretation of Bedrock server stdout as typed events (section 4).

:mod:`cobble.events.patterns` holds the line-recognition primitives shared by
the supervisor (which needs the readiness signal) and the full parser.
"""
